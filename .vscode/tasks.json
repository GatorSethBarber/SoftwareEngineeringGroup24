{
  "version": "2.0.0",
  "tasks": [
    {
      "command": "npx concurrently \"cd GiftCardXChange && ng serve  --open\" \"cd src/server && go run .\"",
      "type": "shell",
      "label": "project run",
      "runOptions": {
        "runOn": "folderOpen"
      }
    }
  ]
}
